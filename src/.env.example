# Optional local signed-updater build settings.
# Leave empty for normal local builds; bun run app:build disables updater artifacts when no private key is set.
TAURI_SIGNING_PRIVATE_KEY=
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
